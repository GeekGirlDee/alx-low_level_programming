#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
 * main - A program that prints the lowercase alphabet in
 * reverse, followed by a new line
 * Return: 0 (pass)
 */
int main(void)
{
	chat alph = 'z';

	while (c >= 'a')
	{
		putchar(c);
		c--;
	}

	putchar('\n');
	return (0);
}
