#include "main.h"
/**
 * add - A program that adds two integers togethr
 * @a: takes in an integer
 * @b: takes in an integer
 * Return: results of a + b
 */
int add(int a, int b)
{
	return (a + b);
}
