#include <stdio.h>
/**
 * main - A program that prints the size of various types on a computer and runs on
 * Return: 0 (Pass)
 */
int main(void)
{
char a;
int b;
long int c;
long long int d;
float e;
printf("Size of a char: %lu byte(s)\n", (unsigned long) sizeof(a));
printf("Size of an int: %lu byte(s)" + b +"\n"
printf("Size o a long int: %lu byte(s)\n", (unsigned long) sizeof(c));
return (0);
}
